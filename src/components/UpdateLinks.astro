---
import BadgeLink from "./BadgeLink.astro";
const { commit, post, note } = Astro.props;

const commitUrl = commit
  ? `https://github.com/nadi-stuti/kosh/commit/${commit}`
  : null;
const postUrl = post ? `/blog/${post}` : null;
const noteUrl = note ? `/notes/${note}` : null;
---

<span>
  {post && <BadgeLink url={postUrl} variant={"note"} text={"Blog Post"} />}
  {commit && <BadgeLink url={commitUrl} variant={"success"} text={"Commit"} />}
  {note && <BadgeLink url={noteUrl} variant={"tip"} text={"Note"} />}
</span>
